---
title: Components
description: Browse the Open Graph image components available in the ogcn registry.
---

Every component is a self-contained React component built with the
Satori-compatible CSS subset, so the exact same code renders as a real PNG via
`next/og` on the server and as a live, scaled preview here in the docs.

<ComponentsList />
